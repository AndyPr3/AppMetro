<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AppMetro.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Fondo del Splash Screen -->
        <item name="android:windowBackground">@color/splash_background</item>
        <!-- Deshabilitar barra de título y hacer pantalla completa -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Deshabilitar la Splash Screen API predeterminada -->
        <item name="android:windowSplashScreenAnimatedIcon">@null</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
    </style>
</resources>